$(document).ready(function(){

	$("#demo").vc3dEye({
	imagePath:"img/slices/", // the location where you’ve put the images.
	totalImages:1858, // the number of images you have.
	imageExtension:"jpg" // the extension of the images. Make sure all the images have same extension.
	});
	
});
